<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include ./partials/_header.ejs %>
  <center>
	  <% if(Object.keys(urls).length > 0) { %>
			<% for(key in urls) { %>
			  <form action="/urls/<%=key%>/delete" method="post">
				<li><%=key%> - <%=urls[key].long_url%> (visits: <%=urls[key].total_visits%>, unique visits: <%= urls[key].unique_visitors.length%>, created: <%=urls[key].birthday%>)<br>
				<a href="/urls/<%=key%>">edit</a> <input type="submit" value="delete"></li>
			  </form>
			<% } %> 
	  <% } else { %>
	  	<p> You don't have any urls. Click the link below to begin. </p>
	  <% } %>
		<a href="/urls/new">Shorten New Url</a>
  </center>
</body>
</html>